# 验证记录与剩余风险

## 已观测结果

2026-09-11，提交 `50c1050ad73ffa2c601131f3408a97e57940d406` 的 macOS CI 成功：
https://github.com/apple1day/nice/actions/runs/34564870675

Xcode 16.4 / iPhone 16 Pro 模拟器 / iOS 18.5 / arm64：
- 完整 App 编译、链接和测试成功。
- CoreTests 21 个、PlaybackModelTests 6 个、VLCDecodeTests 3 个，共 30 个测试，0 失败。
- 真实 VLC 引擎读取本地合成 MP4/MKV，测试验证视频输出存在且播放时间推进。
- `Podfile.lock` 保存的是这次成功 CI 实际生成的解析结果，不是手工猜测的依赖校验值。
- 独立的 Swift 离线策略脚本通过 49 项断言。

本次后续改动新增“挂载前暂停”和“打开途中暂停后忽略晚到播放事件”两项回归测试，
总测试数变为 32（21 + 8 + 3）。后续提交的结果须查看相应 SHA 的 CI，不能把前一个提交的通过结果当成新提交已通过。

## 未消除的警告与验收限制

成功的模拟器测试仍输出 MobileVLCKit 3.7.3 内部 `VLCOpenGLES2VideoView` 的非主线程图层修改警告，
调用栈指向该组件内部的 OpenGLES 缓冲区处理。测试未崩溃且视频输出检查通过，但这不证明该警告无害。
本次没有通过关闭 Main Thread Checker 或运行时方法替换隐藏警告，也没有改用未验收的开发版依赖。
合并前要在目标 iPhone/iOS 上验证连续打开关闭、横竖屏、锁屏恢复和播放稳定性。

模拟器启动还出现系统后台下载服务 XPC 连接日志。当前自动化并未声称覆盖真实的后台下载传输。
飞行模式冷启动、真实网络下载、声音输出、后台下载、磁盘不足和旧版覆盖安装均需物理设备验收。
即使 CI 全部通过，PR 仍保留 Draft，不将此交付标为已完成所有真机测试或可直接上架。
